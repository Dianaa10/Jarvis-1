$(document).ready(function () {
    $('.text').textillate({
        loop: false,
        sync: true,
        in: {
            effect: "bounceIn",
        },
        out: {
            effect: "bounceOut",
        }
    }); // Remove the comma here
});
